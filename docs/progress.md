# colib.h Documentation - Progress

Tracks each chapter's status. Mirrors the role of `tests/progress.md`'s Categories/Test Files
tables - see `CLAUDE.md` in this directory for the workflow. The chapter list itself is planned by
the user, not invented here ahead of time - this table only lists chapters that have actually been
requested/started.

Status values: **not started**, **in progress**, **done** (written and verified against the current
`colib.h`), **stale** (written, but `colib.h` has since changed underneath it - needs a re-pass).

**Date** and **Commit** record when a chapter was last checked against `colib.h` and what state
`colib.h` was in at the time: the base commit hash it was built on, plus a `+local` marker if
`colib.h` had uncommitted changes on top of that commit at verification time (it usually does -
`colib.h` is under active edit). This is what makes "stale" a checkable claim instead of a guess: if
`colib.h` has moved past the recorded commit/state, the chapter needs a re-pass.

**When a `+local` entry's uncommitted changes get committed, drop the `+local` and update the hash to
the new commit** - *if and only if* nothing relevant changed between the two commits beyond what was
already verified (i.e. the commit is exactly "the local changes landed," not new edits on top). This
turns the Commit column into a precise "last actually modified in commit X" marker rather than a
vague "verified around here" one, which is what makes tracking a chapter's accuracy across commit
history possible at all. If something *did* change beyond the verified local diff, that's a fresh
edit - re-verify and record normally (new commit +`local` again if still uncommitted, or the new
commit alone if not).

---

## Chapters

| # | Chapter | Status | Date | Commit | Source material | Notes |
|---|---------|--------|------|--------|------------------|-------|
| 00 | `00_contents.md` - Contents | done | 2026-08-18 | `e5d0c18+local` | This table plus every chapter's own title/scope - no `colib.h` source material, purely an index | Not a chapter in the `01`-`06.0` sense - a one-line-per-chapter table of contents so a new reader has an entry point, split into "Reference" (`01`-`05`) and "Beyond the reference chapters" (`05.1`, `06.0`). Requested by the user 2026-08-18 ("00_contents.md will now have the contents of the docs"). Tracked here anyway, same as every other file in this directory, so it can't silently drift out of sync with what's actually present - `CLAUDE.md`'s Layout section now says as much explicitly. |
| 01 | `01_introduction.md` - Introduction to coroutines & this library | done | 2026-08-15 | `f86175a` | The end of `README.md` - "Introduction" through "Config Macros", excluding the front matter ("On AI"/"Usage"/"Versions") and "Organization" - byte-identical to `colib.h`'s top `DOCUMENTATION` block over that same range | Fully-introductory chapter; content moved here 2026-08-15. Scope corrected same day: "Organization" moved above "Introduction" in `README.md` (repo meta, not library docs) and cut from this chapter; `README.md`'s front matter was never in scope. Re-verified against `colib.h`'s `DOCUMENTATION` block same day - found and fixed real drift (missing `COLIB_ENABLE_DEBUG_CHECKS`/`COLIB_LOG_FUNCTION` rows, stale `COLIB_ENABLE_DEBUG_TRACE_ALL` wording, a tab-vs-spaces table misalignment in `colib.h` itself) - all three (`colib.h`, `README.md`, this chapter) now diff byte-identical over the shared range. `README.md`/`colib.h` still hold their own copies - not yet repointed to this chapter (see `CLAUDE.md`'s "Keeping things in sync"). Later chapters go into detail on specific topics but don't shrink this one. |
| 02 | `02_api.md` - API Reference | done (first pass) | 2026-08-15 | `5dde5f2` | `colib.h`'s `HEADER` section (~line 376-2176): every public declaration + doc comment | Has an ongoing maintenance obligation - see `CLAUDE.md`. First pass covers every declaration in the header as of 2026-08-15; needs re-verification any time a signature or doc comment changes. |
| 03 | `03_execution_model.md` - Execution Model: call vs. sched, the scheduler, semaphores | done | 2026-08-15 | `fd519a6` | `colib.h` implementation: task call/await machinery, `pool_t::sched`/`thread_sched`, the run loop, `sem_t`/`create_sem`/wait/signal | Requested by the user 2026-08-15: what actually happens on call vs. sched, what the scheduler does with ready coroutines, how semaphores fit into that flow. Verified line-by-line against the current implementation, including the caller_state->self-is-unconditional-on-a-real-caller point (double-checked mid-session per user pushback) and the pool_t::sched-vs-co_await-colib::sched modif-inheritance nuance. Extended same day with a `co_yield`-vs-`co_return` explainer and a `colib::yield()` section (with an explicit disambiguation from `co_yield`, since the names invite confusion). Re-verified after the `next_task_state()` TODO landed in `colib.h` (inside `handle_ready()`). Line-number citations were dropped from this chapter entirely 2026-08-17 in favour of symbol references, after a small `sem_internal_t::signal()` edit drifted ~100 of them across the docs at once. |
| 04 | `04_lifetimes.md` - Lifetimes: coroutine frames, killers, semaphores, the pool, modifications | done | 2026-08-17 | `e5d0c18` | `colib.h`: `destroy_state()`, `create_killer()`'s unwind, `sem_t`/`create_sem`/`invalidate_self`, `pool_t`'s ctor/dtor/`clear()`, `modif_t`/`modif_table_t`/`create_modif`, plus the fixed `tests/018-010` UAF as a worked cautionary example | Requested by the user 2026-08-15, scope agreed in two passes: coroutine-frame/killer/semaphore-clear/pool-teardown lifetimes first, then pool/semaphore/modification lifetimes added on top. Verified line-by-line, including a key finding: `sem_t` and (after the 018-010 fix) `modif_t` are deliberately allocated via the global allocator, not the pool's bucket allocator, specifically because both are designed to outlive any one pool - the identical reasoning applied twice, once by design and once as a bugfix. The `create_killer` section was rewritten after user review from an internal-state walkthrough into a user-relevant one: why a killer exists (cancelling a chain from outside, e.g. `create_timeo`), why the three wait-cases it handles are exhaustive (cooperative single-threaded scheduling - the target is never mid-execution when killed), the innermost-first "reverse of call order" destruction ordering, and that killing an already-finished task is a safe no-op, not a UAF. Extended 2026-08-15 during the multithreading research pass with a new invariant, raised by the user and verified against the code: a coroutine is bound to exactly one pool for life via `state->pool`, set in exactly two places in the whole file (`task<T>::await_suspend` on `call`, `external_init_task` on `sched`) and never reassigned - using it with/from a different pool afterward is UB, with exactly one exception (a never-yet-`call`ed-or-`sched`'d "newborn" task, `state->pool == nullptr`, handed to a different pool's `thread_sched`) - raised during a multithreading-design research pass that has since moved to `experiments/ai/colib-multithread/` (kept out of this repo, purely exploratory). Line-number citations dropped from the rest of the chapter 2026-08-17 along with `03`'s (same pass); this row's own Commit bumped to `e5d0c18` since that's the commit those citations were actually removed in - the `state->pool` paragraph is unaffected local content layered on top, not reverified against new colib.h behavior. |
| 05 | `05_platforms.md` - Platforms: Windows/Linux backends, the WIP Unix/kqueue engine | done | 2026-08-15 | `fd519a6` | `colib.h`'s epoll/IOCP/kqueue `io_pool_t`/`timer_pool_t` backends, `io_desc_t`'s three shapes, the wrapper-function blocks, `COLIB_OS_*` selection | Requested by the user 2026-08-15. States kqueue's incomplete status plainly (cross-referencing `tests/BUGS.md` #4/`TODO.md` #1) rather than implying three symmetric backends. Verified line-by-line, including a fresh finding: the Linux and Unix wrapper-function blocks are two separate `#if` blocks, not shared via `#if LINUX \|\| UNIX` - confirmed via diff that they're near-duplicates *not* out of laziness, every difference is exactly where the two backends' `io_desc_t` shapes differ (epoll's `.fd`/`.events` vs kqueue's `.ident`/`.filter`). |
| 05.1 | `05_1_APENDIX_asio.md` - Appendix: implementing an Asio backend | proposal - not decided | 2026-08-15 | `fd519a6` | Fills in `colib.h`'s own commented-out `COLIB_OS_UNKNOWN` reference template with an Asio-based `io_desc_t`/`io_pool_t`/`timer_pool_t` sketch | **Not a description of existing behavior - a design proposal, written for the user to compare against their own idea before anything gets implemented.** Framed around the key insight that Asio's `async_*` ops are completion-based like IOCP, not readiness-based like epoll, so this backend's shape mirrors the Windows backend's (stashed-closure `io_desc_t`, `add_waiter` issues the op) rather than epoll's. Ends with four explicitly open/unsettled design points (how wrapper functions reach the pool's `io_context`, `push_ready` access from inside a completion handler, `clear()`'s interaction with `04_lifetimes.md`'s teardown order, and the single-thread-only constraint) rather than presenting the sketch as finished. Extended with an operation-count survey (verified against the live Asio 1.30 reference docs via WebFetch, not recalled from memory): ~9 core `async_*` verbs across ~12 device types, of which `colib.h`'s current backends match maybe 5 verbs on one device type - flags that "reach parity with today's connect/accept/read/write surface" and "also expose everything Asio can do" are two different, separately-decidable scopes. Followed with a full worked sketch of all 9 verbs as imagined colib wrapper functions (send/receive, send_to/receive_from, wait, resolve, read_at/write_at, read_until), each flagging its own shape mismatch against colib's existing "caller owns the buffer"/byte-count convention where one exists (UDP needs an endpoint param, resolve returns a collection not a byte count, read_until needs its own persistent buffer instead of a caller-supplied one). Revised per user direction: `read`/`write` (and `read_until`, which composes on top) are now C++20 `requires`-constrained templates (`AsyncReadStream`/`AsyncWriteStream`, defined in the appendix since Asio itself only documents these as informal named requirements, not real `concept`s) instead of one function per device type - covers sockets/posix descriptors/Windows handles/serial ports/pipes with a single definition each, narrowing the earlier "9 verbs × 12 types" scope estimate to "a handful of generic functions plus half a dozen genuinely type-specific ones." Second pass after the user pushed back ("is that all?") with real gaps, not restated content: fixed a genuine bug repeated across every sketch (Asio signals EOF as an error code, not a 0-byte return like POSIX/colib's own `read()` - was being collapsed into `ERROR_GENERIC`, silently turning ordinary EOF into a reported failure; also missing `operation_aborted` → `ERROR_WAKEUP` mapping); corrected an earlier understatement that `wait_event` "basically still works" on this backend - it doesn't, since a caller can't hand-build a populated `asio_op_t` the way they hand-build a bare `{fd, events}`, a real loss of colib's "arbitrary event, no wrapper needed" escape hatch; added two new open points (`stop_fd`/`stop_handle` likely aren't needed at all - a real simplification, since Asio's own `close()` self-evicts - and `get_internal_handle()` traced as genuine public API this backend can't honestly support); and closed with an explicit "not covered at all" section (build/test integration, the epoll-wrapped-by-Asio-wrapped-by-colib double-layering tradeoff on Linux, debug/tracing support, no end-to-end worked example) so the appendix doesn't imply completeness it doesn't have. Third pass, driven entirely by user design direction rather than my own review: corrected the whole ownership model - `io_pool_t` was sketched owning a private `asio::io_context`, but the actual intent is a pool *subcontracts* a caller-supplied engine (so plain Asio code can share it and keep running unmodified alongside colib coroutines) - a real architectural improvement, not a variant. First attempt at wiring that through claimed it needed a new `create_pool(asio::io_context&)` overload upstream; user correctly rejected that and pointed at the already-existing `pool_t::user_ptr` escape hatch instead - `io_pool_t` now keeps `colib.h`'s exact unmodified 2-arg constructor and reads the engine out of `pool->user_ptr` lazily, no upstream signature change needed at all. Surfaced one real new correctness bug this ownership change introduces: `handle_ready()`'s single `run_one()` call can no longer be trusted to mean "this pool is idle" on a *shared* engine, since one `run_one()` call might service an unrelated library's handler instead of this pool's own - can silently make `pool_internal_t::run()` return `RUN_OK` while this pool still has live unresolved waiters, a broken contract, not just an efficiency loss; fix sketched (per-pool outstanding-op counter, loop `run_one()` instead of calling it once) but not implemented. Fourth pass: implemented that fix for real rather than leaving it sketched, after the user asked directly whether it was actually resolved. `user_ptr` now holds a small `asio_engine_t{ctx, outstanding}` struct instead of a bare `io_context&`, giving `handle_ready()` a real per-pool idle check to loop `run_one()` against, and giving `asio_op_t` (via a new `pool_t *pool` field, set once in `add_waiter`) the reachability every `push_ready_from`/`--outstanding` pair needed - closing the two previously-open points about engine reachability and idle-detection together, with working code, not just diagnosis. Fifth pass: the user directly challenged the earlier "stop_fd/stop_handle aren't needed" claim - correctly. Traced `stop_fd()`'s real chain (cross-platform `stop_io` → `io_pool_t::force_awake`) and found the mechanism is exactly what this backend's own `force_awake` already provides (synchronous evict-and-wake) - not eliminated, and actually more load-bearing here given `cancel()`'s async-completion pitfall already documented above (dangling `&s` references in `issue`/handler closures if an object is torn down without going through `stop_io` first). Surfaced a genuinely new, still-unresolved gap in the same correction: cancelling *by bare fd/handle* (as opposed to via a killer, which never needed a lookup) works on epoll/IOCP only because those backends maintain their own fd/handle → waiting-`state_t` map; this sketch has no equivalent for `Stream&`/socket identity → `asio_op_t`, so bare-handle cancellation (not killer-driven) doesn't currently have a working path - left open rather than papered over with a second incorrect "it's fine" claim. |
| 06.0 | `06_0_example_chat.md` - Example: a multi-channel chat server and client | done | 2026-08-15 | `74647e5+local` | New, real code in `examples/06_0_chat/` (`chat_common.h`, `chat_server.cpp`, `chat_client.cpp`, `windows.makefile`/`linux.makefile`/`makefile`), not `colib.h` itself - a hub-relay chat server (one `pool_t`, one accept loop dispatching a coroutine per client) plus a chat client (two coroutines per session: one reading the server socket, one reading stdin/parsing `/commands`), covering `NICK`/`JOIN`/`LEAVE`/`MSG`/`LIST`, multi-channel membership, `/switch`, and graceful shutdown | First entry in a new tutorial series (`06_*`), distinct from the `0N` reference chapters - real, runnable example applications paired 1:1 with a chapter each, living in the new top-level `examples/` directory (own `CLAUDE.md`, mirrors `tests/`'s structure but is explicitly not part of the test suite - no assertions, no `BUGS.md` linkage). Requested by the user 2026-08-15 ("let's make some simple apps... chat ap with multiple chats active at a time and parsing /commands... similar to minecraft to switch between channels... implementation is up to you"). Planned via `EnterPlanMode`/`ExitPlanMode` before writing code, given the real architectural decisions involved (server-hub vs. peer-to-peer mesh, wire protocol shape, file layout) - approved plan covered all of it, including a platform constraint researched *before* planning (a dedicated research pass, not assumed): `colib.h`'s Windows/IOCP backend cannot do overlapped I/O on `GetStdHandle(STD_INPUT_HANDLE)`, so `colib::read()` can't target the console directly there, unlike Linux's epoll backend. Solved with `chat_common.h`'s `async_stdin_t` - a background `std::thread` doing blocking console reads, forwarding lines into a `FILE_FLAG_OVERLAPPED` named pipe the pool reads normally - presented in the chapter as a general "bridge any blocking OS resource into a colib.h pool" technique, not a stdin-specific hack. Verified by actually building and running the example on this Windows dev box (not just compiling): drove two real `chat_client` processes against a real `chat_server` process with scripted, timed input, confirmed NICK/JOIN/LEAVE/MSG-relay/LIST/`/switch`/background-channel-marker/clean-mutual-shutdown (both clients exit 0) and abrupt-disconnect (`Process.Kill()`, no `/quit`) survival on the server side. That verification pass found and fixed three real bugs no amount of reading-only review would have caught: (1) the stdin bridge's named pipe needed an explicit `co::ConnectNamedPipe()` handshake before the first read - the very first attempt failed with `ERROR_PIPE_LISTENING`, matching the exact pattern `tests/005-001-io.cpp`'s `test8_io_pipe` already uses; (2) a UTF-8 BOM (`EF BB BF`) prepended to the very first line off the bridged stdin (from the test harness's `Process.StandardInput`, but just as real from a BOM'd text file redirected into stdin on either platform) silently defeated the client's `line[0] == '/'` command check - fixed with a `strip_utf8_bom()` helper applied to the first line of a stream only; (3) `broadcast()`'s member-relay loop originally iterated `g_channels[channel]` live while `co_await`ing a write to each member - a genuine reentrancy-across-a-suspend-point bug (another client joining/leaving/disconnecting the same channel mid-broadcast can mutate the very `std::set` being iterated, undefined behavior despite the whole program being single-threaded), fixed by snapshotting the member list before the loop's first `co_await` and called out in the chapter as a generalizable lesson in its own right (reentrancy-across-a-suspend-point, distinct from - but related to - the genuinely-concurrent-resumption hazards a multithreading design has to reason about, per a research pass since moved to `experiments/ai/colib-multithread/`). `docs/CLAUDE.md`'s "user plans the chapter list" convention applies here too - the doc explicitly lists what's out of scope (auth, persistence, DMs, TLS, a real multi-pane UI, a disconnect-broadcast notice) without proposing or naming `06_1`/`06_2`. |

Further chapters to be planned with the user - not pre-listed here.
